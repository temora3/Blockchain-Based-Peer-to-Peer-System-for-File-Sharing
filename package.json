{
  "name": "ubichain-monorepo",
  "private": true,
  "workspaces": [
    "ubichain"
  ],
  "scripts": {
    "build": "npm -w ubichain run build",
    "dev": "npm -w ubichain run dev",
    "deploy": "hardhat run scripts/deploy.ts --network localhost",
    "compile": "hardhat compile",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.18",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "next": "^15.5.5"
  }
}
